#!/usr/bin/env bash
# This script creates an RSA key pair named 'school' with 4096 bits and passphrase 'betty'

# Generate the key pair with the correct format and store it in the current directory
ssh-keygen -t rsa -b 4096 -f ./school -N "betty"
